// next.config.mjs

/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['websitegrow.co.uk'], // Allow images from this domain
    },
  };
  
  export default nextConfig;
  